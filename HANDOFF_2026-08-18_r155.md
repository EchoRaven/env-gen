# Handoff — 2026-08-18, r155 正在运行

写给下一个 session。**r155 已经起跑**(10:30:53 启动),这是整个 arc 里第一次真正进入用户授权的那个循环。

---

## 1. 目标(用户设定,标准不变)

> 「跑一个实验 → 根据这轮实验尽可能修复所有问题 → 确认没有任何错误修复 → 跑下一轮实验,我都授权。
> 整个 pipeline 完美之后,`/home/haibotong/recursive-agent-papers.zip` 可以开始参考这些文献想象怎么优化」

**你被预先授权跑下一轮,不需要再问。** 论文那一步的前置是 pipeline 跑通,现在还没到。

质量标准(长期不变):**功能完整 + 无 bug + UI 高度相似 + 可泛化**。

### 硬约束(违反过会出事,逐条都是血的教训)

| 约束 | 原因 |
|---|---|
| 修复必须是**可泛化的框架改动**,绝不能是 app 专用 hack 或产品字面量 | 用户明确要求 |
| **推送由用户做**(我的 GitHub egress 是 403) | 见 memory |
| **绝不 `pkill -f <pattern>`**,只按 PID 杀 | 今天第 4 次自杀 shell(exit 144) |
| **绝不同时跑第二个 generation** | 并行会撞默认端口,静默污染 |
| **绝不 `git checkout` 可能有未提交内容的文件** | 会吃掉工作 |
| 解析 PID 和 kill **必须分成两次调用** | `pgrep -f` 会匹配到我自己的 shell |

---

## 2. 现状

### r155(正在跑)

```
启动      2026-08-18 10:30:53
名称      netflix-web-r155        模式 ENVGEN_SINGLE_MILESTONE=0(多里程碑)
模型      gpt-5-6-sol-genai-responses   经 :8900 的 metagen sidecar
PID       2493968  (pgid 2493465)   ← 也写在 r155_PIDS.txt
sidecar   2477711  监听 :8900
日志      /home/haibotong/forgingground-gen/gm_netflix-web-r155.log
产物      agent/generated/netflix-web-r155/
已确认    真实 LLM 调用成功(27.1s / prompt 1463 / completion 1735 tokens)
```

**先查活,再动任何东西**(PID 是精确的,不要用 `pkill -f`):

```bash
ps -o pid,etimes,args --no-headers -p 2493968     # 空输出 = 已退出
curl -sf http://127.0.0.1:8900/health             # sidecar 还在不在
```

### 凭据(这是今天最大的一块解锁)

用户给了 4 个凭据,**只有 mg-api 那类能用**:

    LLM|<appid>|<secret>  × 2   认证通过,但 11/11 模型全部 ModelAccessDenied
                                → app token 不携带模型 entitlement(这是类型属性,不是配置遗漏)
    mg-api-…              × 2   第一把验证通过,smoke test 返回真实 pong

★ **secret 不写进仓库。** 但你可能不需要它:**sidecar 已带着可用的 key 跑在 :8900**,而
`run_netflix.sh` 在端口已健康时会**完全跳过 key 检查**(`run_netflix.sh:72` 的 `if sidecar_healthy`
直接短路;`MG_KEY` 只在第 57 行那条启动分支里才强制)。所以只要 sidecar 还活着,下一轮直接
`NAME=… ./run_netflix.sh` 就行。

sidecar 死了怎么办:脚本第 42 行会 source `/tmp/envgen_key.sh` 作为兜底,但**这个文件当前不存在**
(2026-08-18 已核实),所以兜底是空的 —— :8900 一旦死掉,**只能向用户再要一把 `mg-api-…`**。
别去翻 `~/.bash_history`,那是被策略挡住的。

### 已证明可用的发射链路(今天首次全程实证)

    凭据 → Thrift 传输 → MetaGen 服务 → sidecar(:8900,OpenAI 协议)→ 引擎
    preflight / podman shim / FGEN_PY 覆盖 / 21 张参考图 / 221 工具 / kickoff → implement

### 代码状态

- 分支 `feat/netflix-generality-366-367`,**已全部推完**(用户 10:39:53 push,含本 handoff 那个
  commit;`HEAD == origin/…` = `0674d5a`,`@{u}..HEAD` 为 0)。**不要再去催用户推。**
  以后判断待推数量一律用 `git rev-list --count @{u}..HEAD`,不要相信文档里写死的数字。
- 全量测试 **6479 通过,0 失败**
- 今天新增两个可泛化修复:**#961**(容器 CLI 全路径解析,shim 不再是前置)、**#962**(容器名跨轮
  歧义时返回「未知」而不是猜)

---

## 3. 怎么弄(下一步动作,按顺序)

### 3.1 盯住 r155

```bash
tail -f /home/haibotong/forgingground-gen/gm_netflix-web-r155.log
```

**不要用前台 `sleep` 等**(2.5 分钟会被沙箱墙杀掉);用 `run_in_background: true` 且让命令自己
写日志文件。**等进度,不要等存活**。

#### 「有进度」和「在空转」长什么样

进程活着 **不等于** 在干活。11:24 实测,这一轮同时有两种循环在跑:

    真进度   Orchestrator 的 kickoff poll 行里,"N missing" 在变少、"polls since progress" 归零
    空转     同一个 "N missing" 连着几十 poll 不动,"polls since progress" 一路往上爬

当前状态(登记在案,**下一轮要判定这算不算缺陷**):

    11:21:27  poll 1   3 missing: backend, frontend, verifier   0 polls since progress
    11:23:57  poll 31  1 missing: frontend                     14 polls since progress

    ACTION_STATUS: stop / FINISH  在 53 分钟里累计 234 / 254 次,
    每次 orchestrator 唤醒烧 27–30k prompt token 只吐 55–68 completion token。

这个「醒来 → 发现没有新事件 → stop → 再醒来」的昂贵空转,**要么是已知噪声,要么就是本轮的第一个
发现**。别默认它正常。数一下就知道:

```bash
grep -c "ACTION_STATUS: stop" gm_netflix-web-r155.log
grep "Kickoff phase" gm_netflix-web-r155.log | tail -3
```

#### 怎么算跑完了(而不是死了)

    跑完   进程退出 + 日志停止增长 + agent/generated/netflix-web-r155/ 下产物齐全(见 3.2 清单)
    死了   进程没了但产物清单缺项 —— 去日志尾部找 traceback,别直接重跑
    卡住   进程在、日志在长,但只长空转行(见上)——这是要修的东西,不是要等的东西

判断产物齐不齐用 3.2 的 readout,不要靠肉眼看目录。

### 3.2 跑 readout

```bash
cd /home/haibotong/forgingground-gen
.venv/bin/python tools/readout_run.py netflix-web-r155 | tee /tmp/r155_readout.json
.venv/bin/python tools/readout_run.py --selftest      # 先确认工具本身没瞎
```

★ **r155 会产出 r154 根本没有的证据面**(这是本轮的主要价值,预先登记在案):

    logs/preflight.json                  机器状态 + 出错时的补救           #944
    logs/delivery_gate.jsonl             每次 gate 评估 21 个字段,追加式    #948
    design/visual_gate/rounds.jsonl      capture md5 / 零分原因 / 里程碑 / findings
    design/visual_gate/captures/         每个记录分数对应的那张图           #930
    design/scaffold_overwrites_939.json  两个覆写循环,按页                 #939/#951
    design/lane_page_exposure_946.json   1a 的测量(以前收集不到)          #946
    design/visual_gate/served_build.json #738 的状态文件(全语料 0 个运行有过)

**如果某个文件是空的或不存在,那本身就是发现** —— 见「一个未被观察的检查报告的是『没跑』」。

### 3.3 进入修复循环

对这一轮暴露的每个问题:定位 → 修**框架**(不是 app)→ 写测试 → **植入对照要求变红** → 跑全量套件
→ 提交(附上证据,不是断言)。修完这轮能修的,直接跑下一轮,**已授权**。

---

## 4. 会咬人的陷阱(今天实际踩到的)

1. **删掉目录 ≠ 停掉 stack。** 我删了探针项目并宣布清理完成;它的 3 个容器 / 2 个镜像 / 2 个
   volume(含一个 JWT 私钥)又活了 9 小时,**占着下一轮要用的 3001/8000/5433**。清理后必须查
   **运行时**:`podman ps -a`、`volume ls`、`network ls`、端口。
2. **每一轮的 compose project 都叫 `docker`**(名字取自 `docker/` 目录),所以容器名跨轮相同、
   named volume 跨轮共享。#962 让歧义时返回空而不是猜错;**根治(每轮独立 project 名)仍未做**,
   见 item 351 的取舍分析。
3. **裸名搜索不是定位器。** 这个代码库讨论自己的标识符比使用它们更频繁;要锚定 AST 位置。
4. **一个绿色的测试可能在钉住缺陷。** 今天有个测试断言「两个函数仍然硬编码 docker」,修好它们
   反而让套件变红。非空性要对**合成样本**证明,不要对被监管的那棵树证明。
5. **先量可达性再量严重性。** 发现一条 reset 路径会用随机 project 名起 stack——听着很吓人,
   264 个运行日志里触发 **0 次**。这个测量把一次运行前的高风险重构变成了一条记录。

---

## 5. 未决项

| 项 | 状态 |
|---|---|
| **每轮独立 compose project 名** | 分析完成(item 351),依赖层面安全,但需协调 4 个模块;当时判断不值得在运行前动 |
| **#914 默认值** | 仍关闭,曝光度测量已记录 |
| **1b 选项 B**(integration→lane 传播,CodePlan 式) | 用户已同意作为独立 ticket,未开工 |
| **论文阶段** | 前置未达成;只读了 RAO 全文 + 抽查 FunCoder/Agentless/CodePlan |
| ~~15 个 commit 待推~~ | **已推完**(10:39:53),此行仅留作记录 |
| **kickoff 空转 / frontend 迟迟不 record proposal** | 11:24 观察到,尚未判定是缺陷还是正常噪声 —— 见 3.1 |

---

## 6. 关键文件

    run_netflix.sh                      真正的启动器(仓库根目录,不是 scripts/)
    EXPERIMENTS_PENDING_2026-08-13.md   全部实验记录,item 1–353(今天到 353)
    tools/readout_run.py                单轮读数 + selftest
    tools/stub_llm_server.py            无 key 测发射管路用(它不生成 app,别当成一轮实验)
    tools/podman_shim/                  docker→podman;启动器会自动加到 PATH
    ~/.claude/.../memory/MEMORY.md      跨 session 的教训索引,**开工先读**

---

**一句话交接:r155 是这个 arc 里第一次真正开跑的实验,凭据链已全线打通,证据面比以往任何一轮都宽;
你的工作是把它跑出来的问题一个一个修掉,然后跑下一轮。**
