先读 `/home/haibotong/forgingground-gen/HANDOFF_2026-08-20_r171.md`,再动手。

**前提(不要重新验证,已有产物级证据):模型能力是足够的。** lane 写出的代码是对的 —— 612 字节组件化页面、51 字节 re-export、r167 把后端做到 16/16 探针全绿、r164 检出并修好 44 个真缺陷(r138 检出 0 个还强推了交付)。看起来像"修不完的 bug"的东西,是**框架把已经写对的东西删掉之后的重做成本**。不要去优化 prompt 或 agent 指令,那个方向昨天已经排除。

**第一件事:修 api 模块碰撞。** 这是九轮零交付的根因,已诊断到根,每环有时间戳:

```
11:36:06  框架写 services/api.js   (含 window.NetflixAPI 定义)
11:42:49  lane  写 services/api.jsx (不含)—— 晚 6 分 43 秒,碰撞在检测跑完之后才形成
          App.jsx 导入 api.jsx  →  window.NetflixAPI undefined
          →  九个 UI 流跑不起来  →  两个 UI 门检查常驻  →  门永不变绿
```

修点 `frontend_scaffold.py:11635`(`_reexport_shim_638` 的调用处):文件已存在时也要复查"是否出现了同名兄弟且当前文件不是 shim"。约束见 `#638` 原文 —— **`api.js` 必须保留为真身**,投影代码硬编码 `../services/api.js`。判据:`window.NetflixAPI` 有定义、九个 UI 流能跑。

**第二件事:`#1020`** —— `_env_flag_914()` 默认翻成 `True`,加六个测试的显式 `setenv(...,"0")`。证据已备(item 455,n=5 区间不相交)。

**然后必须跑一轮再叠下一个修复。** 昨天叠了三个,一个都归因不了。

---

**工作方式(昨天用一天换来的):**

- **遇到"为什么会这样 / 该不该改",先 `ls agent/tests | grep -i <关键词>`,再 grep 运行日志原文,最后才读实现。** 昨天读实现七次 0 命中;一条 `ls` 找到四项。`#910`/`#914`/`#939`/`#638`/`#632`/`restore_regressed_chains` 全是已存在的工作,我全部重新发明了一遍。框架还在日志里把答案印了 595 次。
- **确认你的修复在真正运行的路径上。** `#1013` 给一个从不被调用的函数加了守卫,毫无作用。用 `#1015` 的哈希归因(一轮定位),不要靠读函数名推断。
- **上下文余量不足时不做多点编辑。** `#995` 十失败三错误整体回滚,`#1016`、`#1020` 同形。记录下来,停手。
- **只看终值。** 昨天三次 57 分钟的乐观读数被终值推翻。
- **结论要带样本量和区间,不要点估计。** 我从 n=1 说"降 90%",又过度撤回,两次都错。

**硬约束:** 推送由用户做(agent 无 GitHub 出口,SSH/HTTPS 都不通,待推的 24 个提交已打包在 `/tmp/fg-pending.bundle`)· 杀进程只按 PID,禁止 `pkill -f` · 任何时候只跑一轮生成。

**现状:** HEAD `4048220` · 套件 6866 全绿 · 生产代码零改动 · 实验记录 item 354–456 · r171 曾跑到门=1(仅 `unresolved_failed_tasks`),需确认是否已退出并清理运行时。
