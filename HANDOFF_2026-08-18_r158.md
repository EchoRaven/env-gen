# Handoff — 2026-08-18 晚,r158 正在运行

写给下一个 session。取代 `HANDOFF_2026-08-18_r155.md`(那份仍可读,但状态已过期)。

---

## 1. 目标(用户设定,标准不变)

> 「跑一个实验 → 根据这轮实验尽可能修复所有问题 → 确认没有任何错误修复 → 跑下一轮实验,我都授权。
> 整个 pipeline 完美之后,`/home/haibotong/recursive-agent-papers.zip` 可以开始参考这些文献想象怎么优化」

**你被预先授权跑下一轮,不需要再问。** 论文那一步的前置是 pipeline 跑通,现在还没到。

质量标准:**功能完整 + 无 bug + UI 高度相似 + 可泛化**。

用户后来把流程明确成一个循环,照它走:

    监督 → 收割(清点全部缺陷类)→ 甄别(框架自有?后果?正确行为?)
         → 修复 → 记录 → 重启下一轮

**「完全没有能修的地方之后,再起下一轮」** 是用户的原话。

### 硬约束(违反过会出事)

| 约束 | 原因 |
|---|---|
| 修复必须是**可泛化的框架改动**,不能是 app 专用 hack | 用户明确要求 |
| **推送由用户做**(我的 GitHub egress 403) | 见 memory |
| **绝不 `pkill -f <pattern>`**,只按 PID 杀 | 会杀掉自己的 shell |
| **绝不同时跑第二个 generation** | 并行撞端口,静默污染 |
| **植入对照必须验证其变红** | #970 那次差点漏掉真 bug,见下 |

---

## 2. 现状

### r158(正在跑)

```
启动      2026-08-18 18:28:40        PID 3351934 (pgid 3350814) → r158_PIDS.txt
sidecar   2477711  :8900             pkg proxy 127.0.0.1:19080
日志      gm_netflix-web-r158.log    产物 agent/generated/netflix-web-r158/
```

先查活再动任何东西:

```bash
ps -o pid,etimes,args --no-headers -p 3351934     # 空 = 已退出
curl -sf http://127.0.0.1:8900/health             # sidecar
curl -sf -x http://127.0.0.1:19080 https://registry.npmjs.org/   # 包仓库 egress
```

90 分钟时状态:容器 3 个稳定 18 分钟(db/backend healthy)、`api_smoke passed`、
frontend lane 在写页面。**比 r157 任何时刻都靠后。**

### 九个修复,全部已提交(用户会推)

| # | 修复 | 实证 |
|---|---|---|
| 963 | 校验不再冻结事件循环 | r157 构建期间日志连续(r155 是 683s 全黑) |
| 964 | compose spawn 自报家门 | 立刻暴露了 682s 假死和 npm 失败 |
| 965 | acceptance 预算重测 4000→5100 | 语料 154 份 |
| 966 | 唤醒去重竞态补上 | #968 的前置 |
| 967 | 空转判据结构化(读工具账本,不读模型自述) | 退避从死代码变活 |
| **968** | **orchestrator 的 `finish()` 恢复终止语义** | **kickoff 97s(r156 244s),tokens 降 60%+** |
| 969 | `nullable` 不再混进 DDL | r158: 0 次(r157: 4) |
| 970 | 图标补全读得懂压行 import | r158: 0 次(r157: 12) |
| 971 | 技能物化到每个 lane worktree | r158: 0 次(r157: 80) |
| 972 | compose 失败时输出 transcript | 本轮新修,r158 跑的是旧码 |
| — | `run_netflix.sh` 导出包仓库 proxy | 前端首次构建成功 |
| 972 | compose 失败时输出 transcript | #964 只报 rc 不报原因 |
| 973 | postgres ERROR 带上 STATEMENT | 让 `?` 类错误首次可诊断 |
| 974 | FK 归属歧义 —— **r158 死因** | 细化不是猜测;兄弟仍拒绝 |
| 975 | 日志每行双写 | r158 中 34% 的行是重复 |
| 976 | 回执寄给 hub | 82 次/轮,第五次尝试才破 |
| 977 | qmark 占位符 | 三轮六次 `docker_up` 失败 |
| 978 | 补救消息带的是容器 ID | 主动类扫描找到 |
| 979 | 按行数而非语句插入 | 潜伏地雷,26 个语料文件可触发 |
| 980 | async 内 300 秒阻塞调用 | AST 扫描找到,非 grep |
| 981 | 门检查点名具体 flow | r159 死因之一 |
| 982 | 门检查点名具体 page | r159 死因之二 |
| 983 | **阻塞文案随 token 一起带走** | 16 个真实阻塞项里 11 个曾是裸 token |
| 984 | 链拒绝报提交路径而非规范 ID | 全语料第一名的工具失败(466 次) |
| 985 | issue 回复寄给 hub 而非真实作者 | 完成报告丢失 |
| 986 | question 的答案同样误投 | 枚举提前抓到,非等运行 |
| 987 | 错误点名偏移时展示到该位置 | 「at character 255」却只给 200 字符 |
| 988 | `integer?` 不是 SQL 类型 | 三个 session 的悬案 |
| 989 | `default_now` 不是 SQL | 「刻意不修」清单清空 |
| 990 | docker_up 300 秒冻结事件循环 | #980 欠条到期 |
| 991 | 图床 URL 算作退化媒体 | 只修了消费者 |
| 992 | 按钮文案不是伪造数据 | heal 在按钮上放破折号 |
| 993 | 种子图为内联 data URI | **r161/r162 真实死因** |
| 994 | #512 认得该占位符并升级 | 静默降质,无日志痕迹 |
| 995 | 框架修复写不出无法解析的文件 | 16 处路径;首次尝试失败并回滚 |
| 996 | compose 回收期连接被拒重试 | 三个受害面一处覆盖 |
| 997 | DDL 出口断言(#969/#988/#989) | 149 份语料 0 误报 |
| 998 | 一端点一个未关闭任务 | **r162 死因:1 缺陷 → 9 阻塞** |

---

## 3. 怎么弄

### 3.1 盯住运行

**不要用前台 `sleep` 等**(2.5 分钟会被沙箱墙杀掉);用 `run_in_background: true`。
**等进度,不要等存活。**

#### 「有进度」和「在空转」

    真进度   日志在长 + 工具调用是实质动作
    空转     同一状态几十次不动,"polls since progress" 一路爬
    卡住     进程在、日志不长 —— 但先看下面这条再下结论

★★ **静默 ≠ 挂死。** r155 我因为这个误判杀掉了一个正常运行,代价两小时。
进程 block 在子进程上时不烧 CPU,CLOSE-WAIT 只是对端关掉的空闲连接。**先找它可能在等的东西:**

```bash
pgrep -P <PID>                                    # 子进程(构建!)
ps -eo pid,pgid,args --no-headers | awk '$2==<PGID>'
```

#### 怎么算跑完

    跑完   进程退出 + 产物齐全(用 tools/readout_run.py 判,别肉眼看)
    死了   进程没了但产物缺项 → 日志尾部找 traceback
    卡住   见上,先查子进程

### 3.2 收割与甄别

运行结束后系统性清点,别只看最后一个错误:

```bash
grep -ohE "docker_up:[^|']{0,120}" gm_*.log | sed 's/[0-9a-f]\{12,\}/<hash>/g' | sort | uniq -c | sort -rn
grep -ohE "❌ [a-z_]+" gm_*.log | sort | uniq -c | sort -rn
grep -ohE "\[W\] [A-Za-z.]+: [A-Za-z#][^:,(]{0,45}" gm_*.log | sort | uniq -c | sort -rn
```

每一条先判定类别,**不是每条都该修**:

- 框架自有的确定性缺陷 → 修
- 上游失败的**连带后果** → 不修(r157 的 `ui_flow_missing` 就是 #970 的后果)
- **正确行为** → 不修(fail-fast 中止、护栏拒绝写 FRAMEWORK-OWNED 文件、`SUBSTITUTED` 诊断)
- **定位不了的** → 不修,记录(`syntax error at or near "?"`、`default_now`)
- 有类别错误但**无损失证据** → 记录(`Target agent not found: messagebus`,71 次/轮)

---

## 4. 会咬人的陷阱

1. **删掉目录 ≠ 停掉 stack。** 清理后必须查运行时:`podman ps -a`、`volume ls`、端口。
2. **每轮 compose project 都叫 `docker`**,容器名跨轮相同。#962 让歧义时返回空。
3. **裸名搜索不是定位器**,要锚定 AST 位置。
4. **一个绿色的测试可能在钉住缺陷。** `test_why_it_is_only_a_counter_is_recorded` 从 #639
   落地那天起就在钉一个已被推翻的结论,只因没人改注释。
5. **先量可达性再量严重性。**
6. ★ **对照不红 = 没验证。** #970 的测试初稿样本首行是 `import{Link}`(无空格),
   pre-fix 走了另一条分支、在坏代码上通过 —— 差点就那么提交了。换成真实形状后两个 bug 才都被打到。
7. ★ **数日志词频量的是「你看了几次」,不是「问题多严重」。** 我曾把 `ui_flow_missing`
   计数上涨读成补救在空转,实际那个 self-heal 一小时前就停了。要量**阻塞集合**,不是词频。
8. ★ **「它失败了」和「它为什么失败」是两个功能。** #964 报了 rc 却不报 stderr,
   #972 才补上 —— 做完第一个会有已经解决了的错觉。
9. ★★ **测量可能在量日志格式,不是系统行为。** #975 之前日志每行双写,配对相邻行得出
   「66 SAME / 0 DIFFERENT」的压倒性读数 —— 配的是同一次调用的两份拷贝。**一个如此干脆的
   数字,该先触发「什么情况会让它平凡地成立?」,而不是先触发提交。**
10. ★ **grep 找到的是你已经怀疑的东西。** #976 四次失败,是因为一直在找「寻址到 hub 的代码」,
   而 hub 是以**发件人**身份进来的。改成**枚举一个小面**(所有 `target_agent_id=` 赋值)并读
   变量名,一眼就看到。缺陷指纹是**数据流**时,字符串搜索无效。
11. ★ **返回空的扫描不是白扫的。** 五轮类扫描里两轮没找到缺陷 —— 其中一轮(计数代替结构)
   **差一次编辑就会改坏正确代码**并重新引入 #784 防的跨用户误读。先量再改,挡住的是自己。

---

## 6.5 查询面 —— 本轮最值钱的方法

**不要等着撞见缺陷,去问语料。** 把问题从「我注意到了什么」改成「哪一类在所有运行里最频繁」,
产出了本 session 最大的两个发现,而它们都是我在单轮日志里**反复看见并跳过**的。

    门检查细节覆盖    → #983   16 个真实阻塞项里 11 个是裸 token
    工具失败排名      → #984   榜首是框架工具,466 次 / 14 轮
    内部表示泄漏      → 更正了 #984 自己的理由(1088 vs 466 对不上)
    自愈触发频次      → 已知干净(pin 实测幂等)
    消息里的 dict     → 已知干净(是操作者日志)
    prompt 幽灵工具   → 已知干净(宏/阶段/示例代码)

★ 「已知干净」和「未检查」是**两种状态**,只有前者能被继承。三个面现在有确定答案。

★★ **扫描必须报告分母。** 我的第一版 prompt 检查报告「所有 profile 无问题」,而它检查了
**0 个文件**(键名嵌套错了)。空结果集和「查过了没问题」无法区分,而这个区别就是检查的全部
价值。`harvest_run.py` 现在会在解析行数过少时警告 —— 教训写进工具才会被执行。

★★★ **与你的解释矛盾的数字,比印证它的数字有用。** #984 提交一小时后,下一个查询给出
1088 vs 466 的不匹配,推翻了它一半的理由。我不会去复查一个「测试全绿、修复干净」的前提
—— 除非有数字不服。已提交的理由同样会钉住错误信念(陷阱 #4 的高一层形态)。

★★★★ **生成的项目是 git 仓库 —— 修复前的产物都还在历史里。**

    cd agent/generated/<run> && git log --all -- app/database/init/01_init.sql
    git show <hash>:app/database/init/01_init.sql | grep -n '?'

三个 session 里,`?` 和 `default_now` 都因「产物已被自愈修掉」而被归为无法定位。它们一直
在 git 历史里。这个技术二十分钟内连破两案。**每一个「文件已被改掉所以查不了」的死胡同,
现在都能重开。**

★★★★★ **权威来源 vs 便利测量。** 我两次差点用坏查询推翻正确结论:

★★★★★★ **重放真实产物,不要等运行。** 语料里已经有答案:

    #997  全语料 149 份 DDL 喂进新断言        149 通过 / 0 误报
    #998  r163 自己的任务库喂进去重规则       3 真实重复 / 3 拦 / 0 漏
    #995  1575 个后端 .py 喂进写入守卫        0 误伤
    #993  用修好的发射器渲染 12 行            0 外部 URL / 全异 / 全 data:

比等一轮运行快几个数量级,判据更硬。**对会 raise 或会重写代码的修复,这是必须做的** ——
一次误报就是中止运行或损坏应用。

★★★★★★★ **但重放的价值上限,是你选的那个面。** 查 #992 时我抓了所有 JSX 里的 `|| 'x'`,
得到 354 个「会被重写」的字面量(含 CSS 颜色、Tailwind 类名),看着像灾难。实际那个 heal 由
`_HEAL_EXPR` 限定左侧必须是成员表达式 —— 我量的是它根本不碰的面。用真实模式重放:58 个,
且大多可辩护。**同一小时内,同一个陷阱两次。** 挡住我的始终是那句:「这段代码真正处理的是什么?」

    「lane 成功写入 package.json 0 次」→ 假的,成功的文件改动根本不单独记录
    「11,742 个契约列没有 ? 类型」  → 真的,但契约存的是归一化后的类型,不含答案

权威答案在代码里(`_FRONTEND_FRAMEWORK_OWNED` frozenset),不在日志里。
**缺乏证据只有在你先证明「证据本会出现」之后才等于证据缺席** —— 多问一句「成功在这份
日志里长什么样?」,成本是一个 grep。

---

## 5. 未决项

| 项 | 状态 |
|---|---|
| **每轮独立 compose project 名** | 分析完成(item 351),需协调 4 个模块,未做 |
| **`Target agent not found: messagebus`** | 71 次/轮,类别错误但无损失证据,未定位发射点 |
| **`syntax error at or near "?"` / `default_now`** | 各 1 次,无法定位 |
| **preflight 不检查包管理器 egress** | r157 因此烧了 2.3 小时;`run_netflix.sh` 已加探测,但 preflight 本身没有 |
| **确定性失败被当瞬时失败重试** | `_build_with_retry` 对 DNS 不可达也重试 |
| **1b 选项 B**(integration→lane 传播) | 用户已同意作为独立 ticket,未开工 |
| **论文阶段** | 前置未达成 |

---

## 6. 关键文件

    run_netflix.sh                      启动器(已含 pkg proxy 导出)
    launch_netflix.sh                   另一个启动器,proxy 配方的出处(未跟踪)
    EXPERIMENTS_PENDING_2026-08-13.md   全部实验记录,item 1–362
    tools/readout_run.py                单轮读数 + selftest
    tools/ensure_base_images.sh         基础镜像(注意:要 **unset** proxy)
    ~/.claude/.../memory/MEMORY.md      跨 session 教训索引,**开工先读**

**一句话交接:九个框架修复已落地并逐个实证,r158 是第一次真正在做交付门的活;
下一轮之前把这一轮暴露的东西全部清干净,这是用户明确要求的节奏。**
