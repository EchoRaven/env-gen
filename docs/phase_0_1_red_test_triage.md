## Phase 0.1 — Red Test Triage Report

### Summary

- Total red tests: **31**
- **REAL_RED** (fix code): **7**
- **ENV_RED** (fix test infra): **13**
- **OBSOLETE** (delete with sign-off): **11**

Resolution policy:

1. ENV_RED first — mechanical fixture fixes, no risk of masking production bugs.
2. REAL_RED next — each fix exposes a real regression; commit message must cite the test id.
3. OBSOLETE last — every delete requires a second reviewer sign-off, with grep proof in the commit message.

---

### Per-category breakdown

#### REAL_RED — fix production code

| # | test_id | failure_excerpt | bug location | recommended fix |
|---|---|---|---|---|
| 1 | `AnthropicMultimodalTests::test_plain_text_user_message_passes_through` | `AssertionError: [{'type': 'text', 'text': 'just text'}] != 'just text'` (tests/test_anthropic_multimodal.py:32) | `utils/llm.py:1099-1102` — converter always emits content as a list of blocks, no flattening branch for single plain-text turn. | When `blocks` has exactly one `type=="text"` entry and no tool_use / tool_result / image blocks, set `content = blocks[0]["text"]`. Keep list form for multimodal/tool turns so sibling tests stay green. |
| 2 | `TestAuthEnabled::test_auth_required_true_when_token_set` | `AssertionError: False is not true` (tests/test_auth.py:76) | `env_generator/llm_generator/live_monitor_server.py:4482-4484` — `auth_required()` hard-codes `return False` (regression in commit `9d24fcd5`). | Restore env-driven body: `return bool(os.environ.get('ENVGEN_AUTH_TOKEN', '').strip())`. Move any "guest allowed" UX into the `/api/auth/me` handler so the global gate is not disabled. |
| 3 | `TestAuthHTTPE2E::test_logout_invalidates_cookie` | `AssertionError: 200 != 401` (tests/test_auth.py:151) | Same root cause — `_enforce_auth` (`live_monitor_server.py:4620`) short-circuits because `auth_required()` is `False`. `auth_logout_call` and `_SESSIONS` are intact. | Same fix as #2. After restoration, `_enforce_auth` correctly rejects requests with no live session. |
| 4 | `TestAuthHTTPE2E::test_protected_endpoint_returns_401_without_session` | `AssertionError: 200 != 401` (tests/test_auth.py:128) | Same root cause — `/api/projects` is still wired; gate is no-op so it returns 200. | Same fix as #2. `_enforce_auth` will then call `_write_json({'error': 'unauthorized'}, status=401)` at `live_monitor_server.py:4634`. |
| 5 | `TestAuthHTTPE2E::test_static_files_served_without_auth` | `AssertionError: 200 != 401` (tests/test_auth.py:167) | Same root cause — `/api/auth/me` handler (`live_monitor_server.py:4908-4916`) falls through the `if not auth_required()` branch and returns a guest object. | Same fix as #2. With `auth_required()` honest, the authed branch runs and returns 401 when no cookie is present. |
| 6 | `DirMigrationTests::test_existing_shared_crdt_is_auto_migrated` | `AssertionError: None != 'value'` (tests/test_cutover9_dir_migration.py:44) | `env_generator/llm_generator/multi_agent/runtime/hub_registry.py:168` — `old_dir = base_dir / 'shared' / 'hubs'` (should be `'shared' / 'crdt'`). Over-zealous rename in commit `2f865500` made `old_dir == new_dir`, so the legacy migration is a no-op. Docstring at line 166–167 and class docstring at line 38 were also corrupted. | Change line 168 to `old_dir = base_dir / 'shared' / 'crdt'`. Restore line-166 docstring to `Return shared/hubs/, migrating from shared/crdt/ if needed.` Fix class docstring at line 38 (`renamed from shared/crdt/`). |
| 7 | `HubArchitectureTests::test_first_multi_hub_collaboration_loop` | `KeyError: 'tag'` (tests/test_hub_architecture.py:89) | `env_generator/llm_generator/multi_agent/runtime/hubs/codehub/service.py:765-770` returns `{'error': 'release_orchestrator_only', ...}` when `agent != 'orchestrator'`. Test omits `agent=` so it defaults to `'codehub'`, tripping the guard. Production guard is intentional (mirrors `merge_pull_request`). **Sign-off required** — the fix lives in the test, but the design decision deserves a second reviewer to confirm orchestrator-only authorization stays. | At `tests/test_hub_architecture.py:88`, change `hubs.codehub.create_release("v0.1.0", notes="Initial multi-hub release")` to pass `agent="orchestrator"`, matching the `merge_pull_request` call on line 86. Do not relax the production guard. |

#### ENV_RED — fix test infrastructure

| # | test_id | failure_excerpt | env cause | recommended fix |
|---|---|---|---|---|
| 1 | `APIHubTableToolsTests::test_apihub_get_table_breaking_changes_returns_list` | Passes in isolation; suite-level traceback is `RuntimeError: Event loop is closed` from `_run` (tests/test_apihub_table_tools.py:27 calls `asyncio.get_event_loop()`). | Earlier `asyncio.run(...)` in the suite closes the process-global loop; this fixture reuses it. Production symbols `APIHubGetTableBreakingChangesTool` (hub_tools.py:1085) and `SchemaHub.get_table_breaking_changes` (schema_hub.py:315) are live. | Add `def setUp(self): asyncio.set_event_loop(asyncio.new_event_loop())` to `APIHubTableToolsTests` — mirrors the documented pattern at `tests/test_human_chat_mini_loop.py:22-30`. No production change. |
| 2 | `APIHubTableToolsTests::test_apihub_list_tables_returns_dict` | Passes in isolation; same event-loop pollution at line 27. | `APIHubListTablesTool` (hub_tools.py:1054) returns `{"tables": ...}` correctly; `SchemaHub.list_tables` (schema_hub.py:140) intact. | Same fix as #1 — add `setUp` resetting the event loop. |
| 3 | `APIHubTableToolsTests::test_apihub_register_table_consumer_persists` | Passes in isolation; same pollution. | `APIHubRegisterTableConsumerTool` (hub_tools.py:1066-1082) correctly injects `agent=self._agent_id`; `SchemaHub.register_table_consumer` (schema_hub.py:182) intact. | Same fix — add `setUp` resetting the event loop. |
| 4 | `APIHubTableToolsTests::test_apihub_register_table_persists` | Passes in isolation; same pollution. | `APIHubRegisterTableTool` (hub_tools.py:1033) sets `provider=self._agent_id`; `SchemaHub.register_table` (schema_hub.py:103) intact. | Same fix — add `setUp` resetting the event loop. |
| 5 | `APIHubToolsTests::test_apihub_deprecate_endpoint_tool_marks_deprecated` | Not reproducible locally; suspected `_run` helper (tests/test_apihub_tools.py:37-38) is vulnerable via `asyncio.get_event_loop()`. | Tool registered at `hub_tool_surface.py:91`; behavior covered by `tests/test_apihub_strengthen.py::test_deprecate_endpoint_marks_status_and_records_replacement` (PASSED). | Replace `_run` body with a per-call fresh loop (`asyncio.new_event_loop()` + `.close()`) or `asyncio.run(coro)`. Keep test. |
| 6 | `APIHubToolsTests::test_apihub_list_endpoints_tool_returns_dict` | Not reproducible; same fixture vulnerability. | Tool registered at `hub_tool_surface.py:86`; consumed by prompt templates (backend_agent.j2, frontend_agent.j2); accessor exercised by `tests/test_apihub_accessors.py`. | Same fix — rewrite `_run` to use a fresh loop per call. |
| 7 | `APIHubToolsTests::test_apihub_record_contract_test_tool_persists_result` | Not reproducible; same fixture vulnerability. | Tool registered at `hub_tool_surface.py:92`; accessor path exercised by `tests/test_apihub_accessors.py::test_get_contract_test_results_filters_by_endpoint` (PASSED). | Same fix — rewrite `_run` to a fresh-loop pattern. |
| 8 | `APIHubToolsTests::test_apihub_register_endpoint_tool_writes_through` | Not reproducible; same fixture vulnerability. | Most-used apihub write path: `hub_tool_surface.py:94`, `multi_agent/agents/base.py:221`, HTTP handler at `live_monitor_server.py:3989`, plus prompt strings. | Same fix — rewrite `_run`. Do **not** delete; tool is live in prod. |
| 9 | `BugToolsTests::test_bug_close_sets_state_and_evidence` | `AssertionError: tool failed: bug_create: bug_artifacts must include at least one of failing_test, stack_trace, affected_endpoint, affected_files, expected, actual.` | `tests/test_bug_tools.py:127-128` passes `bug_artifacts={}`; production validator at `env_generator/llm_generator/tools/bug_tools.py:66-72` intentionally rejects empty dicts (positively tested by `tests/test_round4_edges.py:231`). | Replace `bug_artifacts={}` at line 127 with `bug_artifacts={'failing_test': 'test_x'}` (mirror `tests/test_round4_edges.py:226`). |
| 10 | `BugToolsTests::test_bug_escalate_marks_state_escalated` | Same `bug_create` validator rejection. | `tests/test_bug_tools.py:138-139` — same empty-artifacts fixture mistake. | Replace `bug_artifacts={}` at line 139 with `bug_artifacts={'failing_test': 'test_x'}`. |
| 11 | `BugToolsTests::test_bug_list_open_lists_only_open` | Same validator rejection. | `tests/test_bug_tools.py:75-78` creates two bugs both with empty artifacts. Validator added in commit `84fdc4dc`. | Change both calls (lines 76, 78) to e.g. `bug_artifacts={'failing_test': 'test_a'}` / `{'failing_test': 'test_b'}`. |
| 12 | `BugToolsTests::test_bug_triage_with_explicit_assignee_overrides_resolver` | Same validator rejection. | `tests/test_bug_tools.py:99-100` — empty artifacts. Sibling `test_bug_triage_assigns_and_records_root_cause` (line 83) passes because it supplies an artifact. | Set `bug_artifacts={'failing_test': 'test_x'}` at line 100. |
| 13 | `BugToolsTests::test_bug_update_state_transitions` | Same validator rejection. | `tests/test_bug_tools.py:117-118` — empty artifacts; validator at `bug_tools.py:66-72` is the intentional contract. | Set `bug_artifacts={'failing_test': 'test_x'}` at line 118. |

#### OBSOLETE — delete (REQUIRES SECOND SIGN-OFF)

Every row below is a delete-only action. The reviewer must verify the grep proof before approving the deletion commit. Commit messages must cite both the deletion commit (`dbbd38ea` or `c6a345a4`) and the live grep evidence.

| # | test_id | grep proof feature is gone | recommended commit message + sign-off requirement |
|---|---|---|---|
| 1 | `CoverageE2ETests::test_dead_endpoint_blocks_then_consumer_registration_unblocks` | `git grep -n 'apihub' env_generator/llm_generator/tools/agent_interaction_tools.py` → 0 hits. `git grep -n 'register_consumer\|dead_endpoint' env_generator/llm_generator/tools/agent_interaction_tools.py` → 0 hits. In-tool dead-endpoint gate deleted in commit `dbbd38ea`; comment block at lines 652–680 documents the deletion. Live owner is `Orchestrator._validate_delivery_gate` → `compute_deliverability` (orchestrator.py:824, runtime/deliverability.py:187). | Commit: `test(coverage_e2e): delete dead-endpoint in-tool-gate test (Cutover 19 removal, dbbd38ea). New owner is compute_deliverability; write a layer-correct test there if desired.` **SIGN-OFF REQUIRED.** |
| 2 | `CoverageE2ETests::test_dead_file_blocks_then_allowlist_unblocks` | `git grep -n 'gate_registry\|app_root' env_generator/llm_generator/tools/agent_interaction_tools.py` → 0 hits. Gate removed in `dbbd38ea`. `mark_path_intentionally_dead` still exists (`gate_registry.py:423`) but `DeliverProjectTool` no longer consults it. | Commit: `test(coverage_e2e): delete dead-file in-tool-gate test (dbbd38ea). gate_registry still live but consulted only via UI / orchestrator paths.` **SIGN-OFF REQUIRED.** |
| 3 | `CoverageE2ETests::test_force_deliver_publishes_audit_event` | `git grep -n 'dead_code_bypass' env_generator/` → only doc-string at `agent_interaction_tools.py:641` and prompt mention at `prompts/v2/orchestrator_agent.j2:618`; no live `publish_event(..., 'dead_code_bypass', ...)` call. `force_deliver` arg still accepted at line 651 but execute() never branches on it. | Commit: `test(coverage_e2e): delete force_deliver audit-event test (event no longer published after dbbd38ea). Follow-up: remove orphan force_deliver parameter from tool schema.` **SIGN-OFF REQUIRED.** |
| 4 | `DeliverProjectRetroGateTests::test_deliver_refuses_without_retro` | `git grep -n 'self\.agent\.hub_registry' env_generator/` → only deletion-rationale comments (`agent_interaction_tools.py:655,674`; `orchestrator.py:1013`); 0 live reads. In-tool retro block deleted in `dbbd38ea`. Live owner: `RetroBeforeDeliverPolicy.handle_finish` (`workflow_policies.py:1309`), invoked via dispatcher at `step_pipeline/tooling.py:204-229`. Lint guard exists at `tests/test_deliver_project_tool_no_dead_hub_registry_reads.py`. | Commit: `test(deliver_retro_gate): delete dead in-tool retro-gate test (dbbd38ea). RetroBeforeDeliverPolicy owns enforcement; write a dispatcher-level test if needed.` **SIGN-OFF REQUIRED.** |
| 5 | `DeliverProjectRetroGateTests::test_deliver_refuses_with_retro_for_different_generation` | Same grep proof as #4 — 0 live `self.agent.hub_registry` reads. Generation-id branch removed in same `dbbd38ea` commit. Live generation-scoped check lives at `workflow_policies.py:1366-1370` (`gate.get_latest_retro_for_generation`). | Commit: `test(deliver_retro_gate): delete generation-id branch test (dbbd38ea). Generation-scoped retro semantics covered by RetroBeforeDeliverPolicy tests.` **SIGN-OFF REQUIRED.** |
| 6 | `DeliverRunhubGateTests::test_deliver_refuses_without_runhub_run` | `git grep -n 'hub_registry' env_generator/llm_generator/tools/agent_interaction_tools.py` → 4 hits, all inside deletion-rationale comments (lines 655, 657, 674, 680); 0 executable reads. RunHub gate removed in `dbbd38ea`; deletion pinned by `tests/test_deliver_project_tool_no_dead_hub_registry_reads.py`. | Commit: `test(deliver_runhub_gate): delete RunHub-required test (dbbd38ea). UI-driven enforcement remains via compute_deliverability.` **SIGN-OFF REQUIRED.** |
| 7 | `DeliverRunhubGateTests::test_deliver_refuses_with_run_before_session` | Execute body (`agent_interaction_tools.py:650-724`) contains no `_session_start_ts` read or RunHub iteration. `git grep -n 'last_successful_run_since' env_generator/` → 1 hit at `runtime/deliverability.py:195`, consumed only by UI-driven `compute_deliverability`. | Commit: `test(deliver_runhub_gate): delete session-cutoff test (dbbd38ea). compute_deliverability owns session-aware enforcement.` **SIGN-OFF REQUIRED.** |
| 8 | `DeliverRunhubGateTests::test_force_deliver_bypasses_runhub_gate` | `force_deliver` declared in tool schema (`agent_interaction_tools.py:636,651`) but never referenced in execute(). `git grep -n 'deliverability_bypass' env_generator/` → 1 hit at `live_monitor_server.py:3507` (UI path), not tool path. Bypass+audit added in `12da8761`, removed in `dbbd38ea`. | Commit: `test(deliver_runhub_gate): delete force_deliver bypass test (dbbd38ea). UI bypass audit covered by live_monitor_server tests.` **SIGN-OFF REQUIRED.** |
| 9 | `RetroE2ETests::test_attempting_deliver_without_retro_then_submit_then_succeed` | `git grep -n 'hub_registry' env_generator/llm_generator/tools/agent_interaction_tools.py` execute() → 0 hits. PR 6 deletion (2026-05-30) documented in source at `agent_interaction_tools.py:652-680`. Live retro owner is `RetroBeforeDeliverPolicy` (`workflow_policies.py:1309`) invoked from `step_pipeline/tooling.py:173,216`. Sibling `test_full_flow_submit_then_deliver` still passes — the deletion is correctness-neutral on the production path; only the explicit "no retro → refused" assertion at line 116 tests the deleted code path. | Commit: `test(retro_e2e): delete dead in-tool retro-gate assertion at line 116 (PR 6, dbbd38ea). Sibling happy-path test_full_flow_submit_then_deliver remains. Do NOT restore the hub_registry gate — lint guard at tests/test_deliver_project_tool_no_dead_hub_registry_reads.py.` **SIGN-OFF REQUIRED.** |
| 10 | `TestSystemMetrics::test_get_token_budget_status_tool` | `git grep -n GetTokenBudgetStatusTool env_generator/` → 0 hits. Class introduced in commit `4ee9a966` (`system_tools.py:352`), deleted in commit `c6a345a4` ("Hub-focus tool model + tool/hub cleanup; keep SystemMetrics"; -217 lines from system_tools.py). Current `system_tools.py` defines only `SystemMetrics`; no `BaseTool` subclasses remain. Sibling `test_get_token_budget_status` (line 49) already covers the underlying method directly. | Commit: `test(system_tools): delete GetTokenBudgetStatusTool wrapper test (c6a345a4 removed the BaseTool subclass; SystemMetrics layer still covered).` **SIGN-OFF REQUIRED.** |
| 11 | `TestSystemMetrics::test_get_token_usage_tool` | `git grep -n GetTokenUsageTool env_generator/` → 0 hits. Class deleted in commit `c6a345a4`. `system_tools.py` contains no `class ...Tool(BaseTool)` definitions. Underlying `SystemMetrics.get_token_usage()` is already exercised by `test_record_and_get_token_usage_single_agent` (line 28), `test_get_token_usage_totals` (line 35), and `test_token_cost_calculated` (line 43). | Commit: `test(system_tools): delete GetTokenUsageTool wrapper test (c6a345a4). SystemMetrics.get_token_usage already covered by three sibling tests.` **SIGN-OFF REQUIRED.** |

---

### Next steps (ordered)

1. **Fix ENV_RED first** (13 tests) — mechanical fixture changes (event-loop reset in `APIHub*` tests, non-empty `bug_artifacts` in `BugTools*` tests). Zero risk of masking a real production bug because no production code changes.
2. **Fix REAL_RED next** (7 tests) — each fix exposes a real regression:
   - 1 production-code edit in `utils/llm.py` (Anthropic plain-text flattening).
   - 4 production-code edits collapse into 1 line at `live_monitor_server.py:4482-4484` (restore env-driven `auth_required()`).
   - 1 production-code edit in `hub_registry.py:168` (`'hubs'` → `'crdt'` + docstring restoration).
   - 1 **test** edit (add `agent="orchestrator"` in `test_hub_architecture.py:88`) — flagged with `second_signoff_required=true` because the design decision (orchestrator-only release authorization) deserves a reviewer's eyes.
   - Every commit message must reference the failing test id it makes green.
3. **Get reviewer sign-off on each OBSOLETE delete BEFORE committing** (11 tests across 4 files). The reviewer must independently re-run the grep proof for each row and confirm the named live owner (`compute_deliverability` / `RetroBeforeDeliverPolicy` / `SystemMetrics`). Deletes should be grouped per source file: `test_coverage_e2e.py` (3), `test_deliver_retro_gate.py` (2; file may become empty), `test_deliver_runhub_gate.py` (3; file may become empty), `test_retro_e2e.py` (1 method), `test_system_tools.py` (2 methods).
4. **Set up CI** at `.github/workflows/test.yml` that runs `pytest tests/` on every PR. The workflow must run on `pull_request` and on `push` to `main`, fail-fast off, and surface the test summary in the PR check. Before enabling required-status, confirm that steps 1–3 have driven the suite to green locally.
5. **Freeze Phase 0.1 baseline** = the passing test count after ENV_RED and REAL_RED fixes have landed and the OBSOLETE deletes have shipped with sign-offs. Record the count, the commit SHA, and the `pytest --collect-only` total in `docs/phase_0_1_baseline.md`. From that point forward, any regression below the baseline is a P0 in the SDLC refactor.

---

### Follow-ups noted in evidence (not required for Phase 0.1 closure)

- `DeliverProjectTool`'s `force_deliver` parameter (`agent_interaction_tools.py:636,651`) is now orphan surface area — execute() never branches on it. Drop the parameter and its docstring as a small follow-up after the OBSOLETE deletes land.
- The class docstring at `multi_agent/runtime/hub_registry.py:38` still references the pre-rename `shared/crdt/` direction incorrectly; fix in the same commit as REAL_RED #6.
- Consider migrating the `_run` helper pattern (deprecated `asyncio.get_event_loop()`) globally — multiple test files share the same vulnerability beyond the ones triaged here.
