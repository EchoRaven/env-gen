[
  {
    "name": "M1-auth-profile",
    "version": "1.0.0",
    "description_slice": "Build the core of an Instagram-style photo/video social app (web, dark theme, left nav rail). MILESTONE M1 \u2014 authentication + user profiles.\n\nPAGES:\n1. Login: username/email + password.\n2. Sign up: email, password, full name, username.\n3. Profile: avatar, username, display name, counts, Edit profile (own).\n\nDATA MODEL (Postgres, every business row owned/scoped per authenticated user):\n- users: id, username (unique), full_name, email (unique), password_hash, bio, avatar_url, created_at.\n- follows: id, follower_id, following_id, created_at (unique pair).\n- posts: id, author_id, media_url, media_type ('image' | 'video'), caption, created_at.\n- likes: id, post_id, user_id, created_at (unique pair).\n- comments: id, post_id, user_id, text, created_at.\n- saves: id, post_id, user_id, created_at (unique pair).\n- messages: id, sender_id, recipient_id, text, created_at.\n\nNEW API ENDPOINTS (FastAPI, JWT auth):\n- POST /auth/register\n- POST /auth/login\n- GET /api/users/me\n- PUT /api/users/me\n- GET /api/users/suggested\n- GET /api/users/{username}\n\nInclude FastAPI backend, React/Vite/Tailwind frontend (login, signup, profile pages, dark Instagram look + left nav rail), Postgres schema for the full data model, and seed demo users."
  },
  {
    "name": "M2-posts-feed",
    "version": "1.1.0",
    "description_slice": "MILESTONE M2 \u2014 building on the ALREADY-DELIVERED earlier milestones (auth/profile already work and are deployed). The endpoints below are NEW; do NOT re-implement existing ones (visible via apihub_list_endpoints as `implemented`). Add POSTS + the HOME FEED.\n\nPAGES:\n- Home feed: reverse-chronological posts (author avatar+username, image, caption, timestamp); right column current-user + Suggested.\n- Create new post: upload image (or URL) + caption.\n- Profile: grid of the user's posts.\n\nDATA MODEL (Postgres, every business row owned/scoped per authenticated user):\n- users: id, username (unique), full_name, email (unique), password_hash, bio, avatar_url, created_at.\n- follows: id, follower_id, following_id, created_at (unique pair).\n- posts: id, author_id, media_url, media_type ('image' | 'video'), caption, created_at.\n- likes: id, post_id, user_id, created_at (unique pair).\n- comments: id, post_id, user_id, text, created_at.\n- saves: id, post_id, user_id, created_at (unique pair).\n- messages: id, sender_id, recipient_id, text, created_at.\n\nNEW API ENDPOINTS (FastAPI, JWT auth):\n- POST /api/posts\n- GET /api/feed\n- GET /api/explore\n- GET /api/users/{username}/posts\n- GET /api/posts/{post_id}\n- DELETE /api/posts/{post_id}\n\nWire these into the existing frontend (feed page, create-post modal, profile post grid)."
  },
  {
    "name": "M3-follow",
    "version": "1.2.0",
    "description_slice": "MILESTONE M3 \u2014 building on the ALREADY-DELIVERED earlier milestones (auth/profile/posts/feed already work and are deployed). The endpoints below are NEW; do NOT re-implement existing ones (visible via apihub_list_endpoints as `implemented`). Add FOLLOW.\n\nPAGES:\n- Profile & feed: Follow/Unfollow button; follower/following counts and lists.\n\nDATA MODEL (Postgres, every business row owned/scoped per authenticated user):\n- users: id, username (unique), full_name, email (unique), password_hash, bio, avatar_url, created_at.\n- follows: id, follower_id, following_id, created_at (unique pair).\n- posts: id, author_id, media_url, media_type ('image' | 'video'), caption, created_at.\n- likes: id, post_id, user_id, created_at (unique pair).\n- comments: id, post_id, user_id, text, created_at.\n- saves: id, post_id, user_id, created_at (unique pair).\n- messages: id, sender_id, recipient_id, text, created_at.\n\nNEW API ENDPOINTS (FastAPI, JWT auth):\n- POST /api/users/{username}/follow\n- DELETE /api/users/{username}/follow\n- GET /api/users/{username}/followers\n- GET /api/users/{username}/following\n\nWire follow/unfollow buttons + followers/following lists into the existing frontend."
  },
  {
    "name": "M4-engagement",
    "version": "1.3.0",
    "description_slice": "MILESTONE M4 \u2014 building on the ALREADY-DELIVERED earlier milestones (auth/profile/posts/feed/follow already work and are deployed). The endpoints below are NEW; do NOT re-implement existing ones (visible via apihub_list_endpoints as `implemented`). Add ENGAGEMENT (likes, comments, saves).\n\nPAGES:\n- Each post: action row like / comment / save, like count, a comments view.\n\nDATA MODEL (Postgres, every business row owned/scoped per authenticated user):\n- users: id, username (unique), full_name, email (unique), password_hash, bio, avatar_url, created_at.\n- follows: id, follower_id, following_id, created_at (unique pair).\n- posts: id, author_id, media_url, media_type ('image' | 'video'), caption, created_at.\n- likes: id, post_id, user_id, created_at (unique pair).\n- comments: id, post_id, user_id, text, created_at.\n- saves: id, post_id, user_id, created_at (unique pair).\n- messages: id, sender_id, recipient_id, text, created_at.\n\nNEW API ENDPOINTS (FastAPI, JWT auth):\n- POST /api/posts/{post_id}/like\n- DELETE /api/posts/{post_id}/like\n- POST /api/posts/{post_id}/comments\n- GET /api/posts/{post_id}/comments\n- POST /api/posts/{post_id}/save\n- DELETE /api/posts/{post_id}/save\n\nWire the like/comment/save action row + comments view into the existing frontend."
  },
  {
    "name": "M5-media-dms",
    "version": "1.4.0",
    "description_slice": "MILESTONE M5 \u2014 building on the ALREADY-DELIVERED earlier milestones (auth/profile/posts/feed/follow/engagement already work and are deployed). The endpoints below are NEW; do NOT re-implement existing ones (visible via apihub_list_endpoints as `implemented`). Add REELS, SAVED, SEARCH, DIRECT MESSAGES.\n\nPAGES:\n- Reels: vertical video player of video posts.\n- Saved: grid of bookmarked posts.\n- Explore/Search: search users.\n- Direct Messages: conversation inbox; open + send text messages.\n\nDATA MODEL (Postgres, every business row owned/scoped per authenticated user):\n- users: id, username (unique), full_name, email (unique), password_hash, bio, avatar_url, created_at.\n- follows: id, follower_id, following_id, created_at (unique pair).\n- posts: id, author_id, media_url, media_type ('image' | 'video'), caption, created_at.\n- likes: id, post_id, user_id, created_at (unique pair).\n- comments: id, post_id, user_id, text, created_at.\n- saves: id, post_id, user_id, created_at (unique pair).\n- messages: id, sender_id, recipient_id, text, created_at.\n\nNEW API ENDPOINTS (FastAPI, JWT auth):\n- GET /api/reels\n- GET /api/users/me/saved\n- GET /api/search/users\n- GET /api/messages/conversations\n- GET /api/messages/{username}\n- POST /api/messages/{username}\n\nWire Reels, Saved, Search/Explore, and Direct Messages pages into the left nav rail."
  }
]